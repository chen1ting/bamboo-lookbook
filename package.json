{
  "name": "bamboo-lookbook",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "upload": "node scripts/uploadPhoto.js",
    "deploy": "npm run build && wrangler deploy",
    "cf-preview": "npm run build && wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.658.0",
    "astro": "^5.1.1",
    "dotenv": "^16.4.5",
    "photoswipe": "^5.4.4",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3",
    "wrangler": "^4.135.0"
  }
}
